A simple Linear regression requires the following library:
#
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
import statsmodels.api as sm
import seaborn as sns
#
Always select independent variable as x axis and dependable as y axis.

#
the general equation of simple linear regression is y = mx + c
where m is slope

# OLS is ordinary least square method of Linear regression model which is found in statmodels library

ols(y,x).fit()

